While studying the GlobalLogic QA AUTO 101 QA Auto Tester course, 
the following tasks were completed:

1.Create a structure of folders and files of the framework.
2.Add all files to the commit and send to the GitHub server 
    using the git push command.
3.Develop tests using the pytest module, using fixtures and labels.
4.Develop tests for testing the HTTP protocol using the pytest and 
    requests modules, using fixtures and tags.
5.Develop tests to test the Github API by creating an API client 
    you've written, using GitHub's API documentation.
6.In the cloned repository, using the results of the previous module, 
    develop tests using the pytest module and sqlite3.
7.In the cloned repository, using the results of the previous module, 
    develop tests using pytest and selenium modules.